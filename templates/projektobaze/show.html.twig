{% extends 'base.html.twig' %}

{% block title %}Projektobaze{% endblock %}

{% block body %}
    <h1>Projektobaze</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projektobaze.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('projektobaze_index') }}">back to list</a>

    <a href="{{ path('projektobaze_edit', {'id': projektobaze.id}) }}">edit</a>

    {{ include('projektobaze/_delete_form.html.twig') }}
{% endblock %}
